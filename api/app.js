'use strict';

// Vercel imports this module for each serverless invocation. Do not call
// app.listen() or start background timers here; local/traditional hosting still
// uses server.js for that behavior.
module.exports = require('../src/app');
